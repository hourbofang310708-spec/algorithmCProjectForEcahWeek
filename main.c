/*1. Given size = 3, capacity = 5, and data = {10, 20, 30, ?, ?}, state the meaningful indexes, allocated indexes, and number of unused slots.
2. For size = 0 and capacity = 4, state whether data[0] is an allocated slot, a meaningful collection element, both, or neither. Explain.
3. Write a linear-search function that returns the first index of target or −1. Test an empty array and an absent target.
4. Trace inserting 15 at position 1 into {10, 20, 30} when capacity is 5. Show every shift and the final size.
5. Trace deleting position 1 from {10, 20, 30, 40}. Show every shift, the returned value, and the final size.*/

